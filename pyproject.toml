[tool.ruff]
line-length = 88
target-version = "py313"
exclude = [
  ".venv",
  "**/node_modules",
]

[tool.ruff.lint]
# Keep linting low-noise for an examples repo: focus on real errors.
select = ["E9", "F63", "F7", "F82"]

[tool.ruff.format]
quote-style = "single"
